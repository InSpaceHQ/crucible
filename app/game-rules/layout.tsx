import type React from "react";

export const metadata = {
  title: "Game Rules",
};

export default function GameRulesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
