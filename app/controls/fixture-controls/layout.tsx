import type React from "react";

export const metadata = {
  title: "Fixture Controls",
};

export default function ControlsFixtureControlsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
