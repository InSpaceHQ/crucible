import type React from "react";

export const metadata = {
  title: "Match Manager",
};

export default function ControlsMatchManagerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
