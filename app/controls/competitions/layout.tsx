import type React from "react";

export const metadata = {
  title: "Competitions",
};

export default function ControlsCompetitionsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
