import type React from "react";

export const metadata = {
  title: "Fixtures",
};

export default function ControlsFixturesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
