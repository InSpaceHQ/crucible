import type React from "react";

export const metadata = {
  title: "Create Competition",
};

export default function ControlsCreateCompetitionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
