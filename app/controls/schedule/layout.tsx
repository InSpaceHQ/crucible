import type React from "react";

export const metadata = {
  title: "Schedule",
};

export default function ControlsScheduleLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
