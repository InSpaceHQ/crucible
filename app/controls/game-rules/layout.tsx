import type React from "react";

export const metadata = {
  title: "Game Rules",
};

export default function ControlsGameRulesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
